name: hello world
on:
  push: 
    paths: 
      - '!README.md'
      - '**.js'
    branches: 
      - 'main'
  workflow_dispatch:
jobs: 
  say-hello: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY=$(date +%y-%m-%d)" >> $GITHUB_ENV
      - run: echo Hello world, to day $TODAY
